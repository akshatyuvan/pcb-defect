# Dev / training / analysis environment (Mac conda env `pcb`, and Colab).
# The Docker serving image uses requirements-serving.txt instead — it is a
# deliberate subset. Anything added here that serving needs must ALSO be
# added there, or the image will build fine and crash at import time.

# --- core numerics / model ---
torch>=2.4,<3
numpy>=1.26,<3
pillow>=10.4

# --- serving ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8

# --- MLOps (dev-side only: registry lives on the host, not in the image) ---
mlflow>=2.16,<4

# --- classical baseline + figures (Day 3 / Day 4 / Day 9) ---
opencv-python-headless>=4.10
matplotlib>=3.9
scikit-learn>=1.5

# --- streaming (Day 6-8). confluent-kafka wraps librdkafka: maintained,
# ships arm64 wheels for both macOS and linux, and exposes explicit
# manual-commit semantics, which Day 7 depends on. ---
confluent-kafka>=2.5

# --- tooling / tests ---
pytest>=8.3
httpx>=0.27
requests>=2.32
tqdm>=4.66
jupyter>=1.1
